export function NotaCuidado({ children }: { children: React.ReactNode }) {
  return (
    <div className="mt-md rounded-borde border border-borde-ciruela-sutil bg-blanco p-md text-ciruela">
      {children}
    </div>
  );
}
